#add bookmarks
if [ -d "$HOME/.bookmarks" ]; then
    export CDPATH=".:$HOME/.bookmarks:/"
    alias goto="cd -P"
fi

# add distrobox to path
if [ -d "$HOME/Documents/code/distrobox/" ]; then
	export PATH="$HOME/Documents/code/distrobox/bin:$PATH"
fi
